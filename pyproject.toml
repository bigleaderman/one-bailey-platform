[tool.poetry]
name = "onebailey-fastapi"
version = "0.1.0"
description = "A FastAPI application for predicting US stock market trends."
authors = ["Your Name <your.email@example.com>"]
license = "MIT"

[tool.poetry.dependencies]
python = "^3.13.9"
fastapi = "^0.95.0"
uvicorn = "^0.22.0"
sqlalchemy = "^2.0.0"
asyncpg = "^0.27.0"
pydantic = "^1.10.0"
psycopg2 = "^2.9.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"